[package]
name = "cuda_npp_sys"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
bindgen = "0.69"

[features]
default = ["static"]
static = []
icc = []
if = []
ig = []
isu = []
